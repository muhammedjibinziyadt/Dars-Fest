rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read for live scores, approved results, notifications & festival data
    match /{document=**} {
      allow read: if true;
      // All mutations are executed securely on the server via Firebase Admin SDK
      allow write: if false;
    }
  }
}
